const announcementList = "/list";
const announcementItem = "/item";
const stats = "/stats";

export enum ERoutes {
  LIST = `${announcementList}`,
  ITEM = `${announcementItem}`,
  STATS = `${stats}`,
}
